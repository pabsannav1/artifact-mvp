# Configuración local para desarrollo
# En producción en Vercel, estas variables se configuran en el dashboard

# Base de datos (para desarrollo local puedes usar una instancia de Neon)
# DATABASE_URL=postgresql://username:password@hostname:5432/database?sslmode=require

# Configuración del servidor
PORT=3000
NODE_ENV=development

# Para habilitar la persistencia en desarrollo, descomenta y configura DATABASE_URL
# Si no se configura, la aplicación funciona en modo memoria (actual)